const numeros = [12, 45, 36, 93, 28, 22, 378, 237, 4, 34, 190];

for (let i = 0; i < numeros.length; i++) {
    const numero = numeros[i];
    if (numero % 2 === 0) {
        console.log(numero);
    }
}